#!/bin/bashn# validate-workflow.sh - Ensure all workflow steps completed successfullynnecho "🔍 Validating workflow completion..."nn# Check required filesnREQUIRED_FILES=("initial.md" "templates/prp-base-updated.md" "*.prp")nMISSING_FILES=()nnfor pattern in "${REQUIRED_FILES[@]}"; don    if ! ls $pattern >/dev/null 2>&1; thenn        MISSING_FILES+=("$pattern")n    findonennif [ ${#MISSING_FILES[@]} -gt 0 ]; thenn    echo "❌ ERROR: Missing required files:"n    for file in "${MISSING_FILES[@]}"; don        echo "   - $file"n    donen    echo ""n    echo "   Workflow incomplete. Please run again."n    exit 1nfinnecho "✅ All required files present"necho "✅ Workflow validation passed"n